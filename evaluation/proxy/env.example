# 裁判网关配置模板
#
#   cp env.example .env  然后按需切换上游，再 ./start.sh
#   .env 被 .gitignore 忽略（.gitignore:138 的 .env 匹配任意目录），可直接写真 key。
#   本文件不带前导点，会进 git —— 不要往这里写真 key。
#
# 换上游只改这个文件，config.yaml 一个字不用动。

# ═══════════════════════════════════════════════════════════════
# 上游：二选一，解开要用的那组，其余注释掉
# ═══════════════════════════════════════════════════════════════
#
# UPSTREAM_MODEL = LiteLLM 的 provider 前缀 + 上游认识的模型名。
# 前缀只决定用什么协议访问 UPSTREAM_BASE_URL，不会出现在发给上游的 body 里。
#
# 两类上游的模型名格式不同，这是唯一容易踩错的地方：
#   OpenRouter    ID 自带 vendor 前缀    anthropic/claude-sonnet-4.6
#   DeepSeek 等   裸名                               deepseek-v4-pro

# ── A. 直连 OpenRouter ───────────
# openrouter/ 是 LiteLLM 原生 provider，BASE_URL 留空即可。
# UPSTREAM_MODEL=openrouter/anthropic/claude-sonnet-4.6
# UPSTREAM_BASE_URL=
# UPSTREAM_API_KEY=sk-or-v1-xxxx

# ── B. DeepSeek，走它的原生 Anthropic 端点 ─────────────
UPSTREAM_MODEL=anthropic/deepseek-v4-pro
UPSTREAM_BASE_URL=https://api.deepseek.com/anthropic
UPSTREAM_API_KEY=sk-xxxx

# ═══════════════════════════════════════════════════════════════
# 本网关自己
# ═══════════════════════════════════════════════════════════════

# verifier 容器访问本网关用的 key。
# evaluation/.env 的 JUDGE_ANTHROPIC_API_KEY 要填成一样的值。
PROXY_MASTER_KEY=sk-judge-local

PROXY_PORT=4100
PROXY_WORKERS=4

# ERROR / INFO / DEBUG。排查转发问题时调 DEBUG 能看到实际发给上游的请求。
PROXY_LOG=ERROR
